// Backward-compatible alias. HearMeOut now uses the persona-neutral SPMT
// command path; AthenaOS is one persona/integration layered on top of it.
export { POST } from '../../persona/commands/route';
